/**
 * ESLint from commandline
 *  - find (eslint <fileName.js>)
 *  - fix (eslint fileName.js --fix)
 */
const   testMethod = () => {
    const x = 1
const y = x + 1;

    return x + y;
};

export default testMethod;

/** // Original code
const   testMethod = () => {
    const x = 1
const y = x + 1;

    return x + y;
};
*/
